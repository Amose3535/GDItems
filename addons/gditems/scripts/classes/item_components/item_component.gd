# item_component.gd
@abstract
extends Resource
class_name ItemComponent
## The base component class which defines a trait of an item.
##
## Even if empty, this class acts as interface with the Item's ECS to allow for detection of item components
